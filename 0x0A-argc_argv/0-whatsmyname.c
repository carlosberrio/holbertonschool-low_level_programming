#include <stdio.h>

/**
 * main - Imprimir el nombre de un programa
 * @argc: número de argumentos
 * @argv: array of arguments
 * Return: 0
 */

int main(int __attribute__((unused)) argc, char *argv[])
{
printf("%s\n", argv[0]);
return (0);
}
