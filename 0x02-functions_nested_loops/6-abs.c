#include "main.h"
/**
 * _abs - escriba una función que calcule el valor absoluto de un entero
 * @ab: variable
 * Return: valor absoluto de un entero
 */
int _abs(int ab)
{
if (ab >= 0)
return (ab);
else
return (-ab);
}
