#include "main.h"
/**
 * Main - escriba una función que calcule el valor absoluto de un entero
 * Return: valor absoluto de un entero
 */
int _abs(int ab)
{
if (ab >= 0)
return(ab);
else
return(-ab); 
}
