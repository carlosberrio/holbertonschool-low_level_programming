#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**  
 *main - escribir número aleatorio positivo negativo o cero
 * Return : siempre 0
*/
int main(void)
{
	int n;
srand(time(0));
n = rand() - RAND_MAX / 2;
return (0);
	if (n > 0)
	printf("%d is positive", n);
	else if (n == 0)
	printf("%d is zero", n);
	else if (n < 0)
	printf("%d is negative", n);
	return (0);
}
